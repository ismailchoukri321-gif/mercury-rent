"use client";
import Link from "next/link";
import { Menu, Phone } from "lucide-react";
import { useEffect } from "react";
import { usePathname } from "next/navigation";
import { WhatsAppButton } from "@/components/WhatsAppButton";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
export function Header() { const pathname = usePathname(); const english = pathname === "/en" || pathname.startsWith("/en/"); const prefix = english ? "/en" : ""; const isHome = pathname === "/" || pathname === "/en"; useEffect(() => { const sections = Array.from(document.querySelectorAll("main section")); sections.forEach((section) => section.classList.add("site-reveal")); const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add("site-reveal-visible"); observer.unobserve(entry.target); } }), { threshold: 0.12 }); sections.forEach((section) => observer.observe(section)); return () => observer.disconnect(); }, [pathname]); return <><header className={`${isHome ? "absolute inset-x-0 top-0 z-20 bg-ink/25 backdrop-blur-sm" : "bg-ink"} text-white`}><div className="shell flex h-20 items-center justify-between gap-4"><Link href={prefix || "/"} className="flex h-14 w-44 items-center overflow-hidden rounded-md"><img src="/mercury-rent-logo.png" alt="Mercury Rent" className="h-full w-full object-cover"/></Link><nav className="hidden gap-8 text-sm md:flex"><Link href={`${prefix}/catalogue`}>{english ? "Our cars" : "Nos véhicules"}</Link><Link href={`${prefix}/#services`}>Services</Link><Link href={`${prefix}/#contact`}>Contact</Link></nav><div className="flex items-center gap-3"><a href="tel:+212665362442" className="hidden items-center gap-2 text-sm lg:flex"><Phone size={16}/> +212 665 362 442</a><LanguageSwitcher/><Menu className="md:hidden"/></div></div></header><WhatsAppButton/></>; }
